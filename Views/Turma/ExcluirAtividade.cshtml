@model DTOModel
@{
    ViewData["Title"] = "Excluir";
}

<div class="d-flex justify-content-between">
    <div class="d-flex">
        <p>Usuário Autenticado:
        <p class="ms-2">@Model.Usuario.Nome</p>
        </p>
    </div>
    <div class="d-flex justify-content-between">
        <a role="button" class="btn btn-primary me-5" asp-action="Index" asp-controller="Perfil"
            asp-route-idProf="@Model.Usuario.Id">Home</a>
        <a role="button" class="btn btn-secondary" asp-action="Index" asp-controller="Home">Sair</a>
    </div>
</div>

<div class="mb-3">
    <h1>Tem certeza que deseja excluir a Atividade @Model.Atividade.NomeAtividade</h1>
</div>

<form asp-action="ValidarExclusaoAtv" asp-controller="Turma" method="post" asp-route-idAtividade="@Model.Atividade.Id">
        <div class="d-flex justify-content-center w-100">
      <button type="submit" class="btn btn-success mx-2" name="excluirAtividade" value="SIM">Excluir</button>
      <button type="submit" class="btn btn-danger mx-2" name="excluirAtividade" value="NAO">Cancelar</button>
    </div>
</form>